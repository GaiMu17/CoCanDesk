# CoCanDesk

CoCanDesk 是一个用于查看和控制小电拼设备的 macOS 状态栏应用。

## 隐私与设备添加

仓库不会内置任何用户设备地址或 MCP 连接信息。首次运行后，请在应用里手动添加自己的设备连接地址；分享给其他用户时，他们也需要添加自己的设备。

## 打包

```sh
./scripts/package_app.sh
```

打包产物会生成在本地 `dist/` 目录，这个目录不会提交到 GitHub。
